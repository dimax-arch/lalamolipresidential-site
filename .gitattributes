# Normalize line endings: store text as LF in the repo and check out LF everywhere.
# Prevents whole-file CRLF churn from Windows/WSL editors.
* text=auto eol=lf

# Binary assets — never touch these.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
